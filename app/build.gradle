apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "financialcalculators.tarun.com.financalculator"
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 7
        versionName "1.6"
    }

    signingConfigs {
        release {
            storeFile file('C:/Users/Tarun/OneDrive/keystores/android.jks')
            storePassword "wIn3sw@GG3r"
            keyAlias 'MyAndroidKey'
            keyPassword "wIn3sw@GG3r"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.0.0'
    compile 'com.google.android.gms:play-services:7.0.0'
    compile files('libs/YahooFinanceAPI-1.3.0.jar')
}
